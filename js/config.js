// ---------------------------------------------------------------------------
// TEMPLE CONFIGURATION
// ---------------------------------------------------------------------------
// This is the ONE place to change temple branding and links.
// A nontechnical volunteer can safely edit the values between the quotes below.
// Do not add commas or quotes in the wrong place; keep the structure the same.
// After editing, reload the page to see the change.
// ---------------------------------------------------------------------------

export const CONFIG = {
  // Shown as the small line above the app title. Leave "" to hide.
  templeName: '',

  // Path or URL to the temple logo shown in the header. Leave "" to hide.
  // Example: 'assets/images/temple-logo.png'
  templeLogo: '',

  // The temple's main website. Powers the "Back to Temple Website" link.
  // Leave "" to hide that link.
  templeWebsiteUrl: '',

  // The temple's EXISTING calendar page. Powers the "View the Temple Calendar"
  // link. This app does NOT create its own calendar. Leave "" to hide the link.
  templeCalendarUrl: '',

  // Optional printable coloring / activity page (PDF or web page).
  // Leave "" to hide the "Printable activity page" link.
  activitySheetUrl: '',

  // Footer message shown at the bottom of the app.
  footerMessage:
    'Made with love for our kids — check with an elder or priest before treating this as the final word 🙏',

  // Who reviewed/approved the content (optional). Leave "" to hide.
  contentReviewAttribution: '',

  // Respectful disclaimer shown near the top of the app.
  // The temple may reword this.
  disclaimer:
    'Festival traditions and practices may vary by family, region, and temple. ' +
    'This content is intended as a child-friendly introduction and should be ' +
    'reviewed with a parent, elder, teacher, or priest.',
};
